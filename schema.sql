CREATE TABLE location (search_query VARCHAR(256) NOT NULL,
                formatted_query VARCHAR(256) NOT NULL,
                latitude FLOAT NOT NULL,
                longitude FLOAT NOT NULL
                )

